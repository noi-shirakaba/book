<h1>書籍一覧</h1>
<% @book.each do |book| %>
  <div>
    <ul>
      <li><%= link_to book.title, book_path(book.id) %></li>
      <li><%= link_to book.label, edit_book_path(book.id) %></li>
      <li><%= book.author %></li>
      <li><%= book.label %></li>
    </ul>
  </div>
<% end %>